StoryBreak - Brand Assets
=========================

Place these files here for a fully branded build:

  splash.png   - Startup splash screen for PyInstaller (optional)
  logo_text.png - Header logo (transparent PNG, any height ~40px) - not yet used in UI

If missing, the app still runs with the default styled "StoryBreak" text header.
